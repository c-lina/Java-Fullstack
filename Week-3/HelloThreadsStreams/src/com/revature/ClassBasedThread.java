package com.revature;

//This custom thread extends the thread class in order override the run method

public class ClassBasedThread extends Thread{
    @Override
    public void run() {
        //We could have defined a custom thread functionality here...
        //But we'll have the InterfaceBasedThread instead
    };
}
